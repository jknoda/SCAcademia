diff --git a/frontend/src/components/home/home.component.html b/frontend/src/components/home/home.component.html
index 1312976..3c20909 100644
--- a/frontend/src/components/home/home.component.html
+++ b/frontend/src/components/home/home.component.html
@@ -20,7 +20,7 @@
     <section class="hero">
       <h2>{{ getHomeTitle() }}</h2>
       <p>{{ getHomeSubtitle() }}</p>
-      <div class="academy-info">
+      <div class="academy-info" *ngIf="!isAluno()">
         <p><strong>Nome da Academia:</strong> {{ currentUser.academy.name }}</p>
         <p><strong>Email de Contato:</strong> {{ currentUser.email }}</p>
         <p><strong>Seu Perfil:</strong> {{ getRoleLabel() }}</p>
@@ -31,6 +31,88 @@
       <span class="badge success">{{ getRoleLabel() }} ativo</span>
     </section>
 
+    <section *ngIf="isAluno()" class="student-progress-section">
+      <div class="section-heading">
+        <div>
+          <h3>{{ getStudentProgressHeading() }}</h3>
+          <p class="muted">{{ getStudentProgressSubheading() }}</p>
+        </div>
+      </div>
+
+      <div *ngIf="isUsingCachedProgressData" class="status-message success">
+        Exibindo dados em cache (offline). ├Ültima sincroniza├º├úo: {{ getFormattedLastProgressSync() }}
+      </div>
+
+      <div *ngIf="isLoadingStudentProgress" class="student-progress-grid">
+        <article class="feature-card skeleton-card" *ngFor="let card of [1,2,3,4]">
+          <div class="skeleton-line skeleton-title"></div>
+          <div class="skeleton-line"></div>
+          <div class="skeleton-line skeleton-short"></div>
+        </article>
+      </div>
+
+      <div *ngIf="hasStudentProgressError" class="status-message error">
+        {{ studentProgressErrorMessage }}
+        <button class="action-btn" (click)="loadStudentProgressDashboard(true)">Tentar novamente</button>
+      </div>
+
+      <div
+        class="student-progress-grid"
+        *ngIf="!isLoadingStudentProgress && !hasStudentProgressError && studentProgressDashboard"
+      >
+        <article class="feature-card student-progress-card">
+          <h4>Evolu├º├úo do M├¬s</h4>
+          <p class="summary-number">{{ studentProgressDashboard.cards.evolucaoMes.currentMonthPresentCount }}</p>
+          <p class="muted">
+            M├¬s anterior: {{ studentProgressDashboard.cards.evolucaoMes.previousMonthPresentCount }}
+            ┬À Delta: {{ studentProgressDashboard.cards.evolucaoMes.delta >= 0 ? '+' : '' }}{{ studentProgressDashboard.cards.evolucaoMes.delta }}
+          </p>
+          <button class="action-btn" (click)="openStudentProgressDetails('evolucao')">Ver detalhes</button>
+        </article>
+
+        <article class="feature-card student-progress-card">
+          <h4>Frequ├¬ncia</h4>
+          <p class="summary-number">{{ studentProgressDashboard.cards.frequencia.attendancePercentage90d }}%</p>
+          <p class="muted">
+            {{ studentProgressDashboard.cards.frequencia.presentCount90d }} presen├ºas em {{ studentProgressDashboard.cards.frequencia.totalCount90d }} sess├Áes (90 dias)
+          </p>
+          <p class="muted" *ngIf="studentProgressDashboard.cards.frequencia.nextClass">
+            Pr├│xima aula: {{ studentProgressDashboard.cards.frequencia.nextClass?.turmaName }} ┬À
+            {{ studentProgressDashboard.cards.frequencia.nextClass?.sessionDate | date: 'dd/MM/yyyy' }}
+            {{ studentProgressDashboard.cards.frequencia.nextClass?.sessionTime }}
+          </p>
+          <button class="action-btn" (click)="openStudentProgressDetails('frequencia')">Ver detalhes</button>
+        </article>
+
+        <article class="feature-card student-progress-card">
+          <h4>Coment├írios do Professor</h4>
+          <p class="muted" *ngIf="studentProgressDashboard.cards.comentariosProfessor.latest as latest">
+            "{{ latest.content }}"
+          </p>
+          <p class="muted" *ngIf="!studentProgressDashboard.cards.comentariosProfessor.latest">
+            Ainda n├úo h├í coment├írios registrados.
+          </p>
+          <p class="muted">
+            Total de coment├írios: {{ studentProgressDashboard.cards.comentariosProfessor.totalComments }}
+          </p>
+          <button class="action-btn" (click)="openStudentProgressDetails('comentarios')">Ver detalhes</button>
+        </article>
+
+        <article class="feature-card student-progress-card">
+          <h4>Faixa e Conquistas</h4>
+          <p class="summary-number">{{ studentProgressDashboard.cards.faixaConquistas.currentBelt || '-' }}</p>
+          <p class="muted">
+            Conquistada em {{ studentProgressDashboard.cards.faixaConquistas.beltDate ? (studentProgressDashboard.cards.faixaConquistas.beltDate | date: 'dd/MM/yyyy') : '-' }}
+          </p>
+          <p class="muted">
+            Federa├º├úo: {{ studentProgressDashboard.cards.faixaConquistas.isFederated ? 'Ativo' : 'N├úo federado' }}
+          </p>
+          <p class="muted">Badges: {{ studentProgressDashboard.cards.faixaConquistas.totalBadges }}</p>
+          <button class="action-btn" (click)="openStudentProgressDetails('faixa')">Ver hist├│rico de faixas</button>
+        </article>
+      </div>
+    </section>
+
     <section class="actions" *ngIf="isProfessor()">
       <h3>Pr├│ximas A├º├Áes</h3>
       <div class="action-buttons">
@@ -162,6 +244,128 @@
       </div>
     </section>
 
+    <div class="student-progress-modal" *ngIf="selectedStudentProgressDetail && studentProgressDashboard">
+      <div class="student-progress-modal-backdrop" (click)="closeStudentProgressDetails()"></div>
+      <div class="student-progress-modal-content" role="dialog" aria-modal="true">
+        <div class="section-heading">
+          <h3>Detalhes</h3>
+          <button class="action-btn" (click)="closeStudentProgressDetails()">Fechar</button>
+        </div>
+
+        <div *ngIf="selectedStudentProgressDetail === 'evolucao'">
+          <h4>Sua Evolu├º├úo</h4>
+          <p class="muted">Sua tend├¬ncia de progresso nas ├║ltimas semanas</p>
+          
+          <!-- Story 4-2: Embed chart component -->
+          <div class="chart-wrapper">
+            <app-student-progress-chart 
+              [weeklyData]="studentProgressWeeklyData"
+              [period]="'current'">
+            </app-student-progress-chart>
+          </div>
+
+          <!-- Fallback to monthly series if chart not rendered -->
+          <div class="history-list" *ngIf="studentProgressWeeklyData.length < 2">
+            <div class="history-item" *ngFor="let item of studentProgressDashboard.cards.evolucaoMes.monthlySeries">
+              <div>
+                <p><strong>{{ item.monthStart | date: 'MMM/yyyy' }}</strong></p>
+                <p class="muted">{{ item.presentCount }} presen├ºa(s) em {{ item.totalCount }} sess├Áes</p>
+              </div>
+            </div>
+          </div>
+        </div>
+
+        <div *ngIf="selectedStudentProgressDetail === 'frequencia'">
+          <h4>Seu Hist├│rico de Frequ├¬ncia</h4>
+
+          <div class="attendance-actions">
+            <div class="attendance-filters">
+              <label>
+                De
+                <input type="date" [(ngModel)]="attendanceFilterFrom" />
+              </label>
+              <label>
+                At├®
+                <input type="date" [(ngModel)]="attendanceFilterTo" />
+              </label>
+              <button class="action-btn" (click)="applyAttendanceFilter()">Filtrar</button>
+              <button class="action-btn" (click)="clearAttendanceFilter()">Limpar</button>
+            </div>
+            <button class="action-btn" (click)="exportAttendanceCsv()" [disabled]="attendanceHistory.length === 0">
+              Exportar CSV
+            </button>
+          </div>
+
+          <div *ngIf="attendanceCurrentStreak >= 2" class="status-message success">
+            ­ƒöÑ {{ attendanceCurrentStreak }} aulas sem faltar! {{ attendanceCurrentStreakDays }} dias consecutivos.
+          </div>
+
+          <div *ngIf="attendanceHistoryWarningBelow70" class="status-message warn">
+            ÔÜá´©Å Sua frequ├¬ncia est├í abaixo de 70%. Pr├│ximas 3 aulas s├úo importantes para recupera├º├úo.
+          </div>
+
+          <p class="muted attendance-summary">Frequ├¬ncia atual: {{ attendanceHistoryPercentage }}%</p>
+
+          <p *ngIf="isLoadingAttendanceHistory" class="muted">Carregando hist├│rico...</p>
+          <div *ngIf="attendanceHistoryErrorMessage" class="status-message error">{{ attendanceHistoryErrorMessage }}</div>
+
+          <div class="history-list" *ngIf="!isLoadingAttendanceHistory && !attendanceHistoryErrorMessage && attendanceHistory.length > 0">
+            <div class="history-item" *ngFor="let item of attendanceHistory">
+              <div class="attendance-row">
+                <p><strong>{{ item.sessionDate | date: 'dd MMM' }}</strong></p>
+                <p class="muted">{{ item.turmaName }}</p>
+              </div>
+              <p class="attendance-status" [ngClass]="getAttendanceStatusClass(item.status)">
+                {{ getAttendanceStatusLabel(item.status) }}
+              </p>
+            </div>
+          </div>
+
+          <div *ngIf="!isLoadingAttendanceHistory && !attendanceHistoryErrorMessage && attendanceHistory.length === 0" class="status-message success">
+            Nenhum registro de frequ├¬ncia encontrado para o per├¡odo selecionado.
+          </div>
+
+          <div class="attendance-pagination" *ngIf="attendanceHistoryTotal > attendanceHistoryLimit">
+            <button class="action-btn" (click)="goToPreviousAttendancePage()" [disabled]="!canLoadPreviousAttendancePage">
+              Semana anterior
+            </button>
+            <p class="muted">{{ getAttendanceRangeLabel() }}</p>
+            <button class="action-btn" (click)="goToNextAttendancePage()" [disabled]="!canLoadNextAttendancePage">
+              Pr├│xima semana
+            </button>
+          </div>
+        </div>
+
+        <div *ngIf="selectedStudentProgressDetail === 'comentarios'">
+          <h4>Timeline de coment├írios</h4>
+          <div class="history-list">
+            <div class="history-item" *ngFor="let item of studentProgressDashboard.cards.comentariosProfessor.timeline">
+              <div>
+                <p><strong>{{ item.professorName }}</strong></p>
+                <p class="muted">{{ item.content }}</p>
+                <p class="muted">{{ item.createdAt | date: 'dd/MM/yyyy HH:mm' }}</p>
+              </div>
+            </div>
+          </div>
+        </div>
+
+        <div *ngIf="selectedStudentProgressDetail === 'faixa'">
+          <h4>Hist├│rico de faixas</h4>
+          <div class="history-list">
+            <div class="history-item" *ngFor="let item of studentProgressDashboard.cards.faixaConquistas.beltHistory">
+              <div>
+                <p><strong>{{ item.belt }}</strong></p>
+                <p class="muted">Data: {{ item.receivedDate | date: 'dd/MM/yyyy' }}</p>
+                <p class="muted">Promovido por: {{ item.promotedBy || '-' }}</p>
+                <p class="muted">Dura├º├úo: {{ formatBeltDuration(item.durationDays) }}</p>
+                <p class="muted" *ngIf="item.notes">Observa├º├Áes: {{ item.notes }}</p>
+              </div>
+            </div>
+          </div>
+        </div>
+      </div>
+    </div>
+
     <section *ngIf="isResponsavel()" class="actions">
       <h3>Pr├│ximas A├º├Áes</h3>
       <div class="action-buttons">
diff --git a/frontend/src/components/home/home.component.scss b/frontend/src/components/home/home.component.scss
index 13eaaee..4873803 100644
--- a/frontend/src/components/home/home.component.scss
+++ b/frontend/src/components/home/home.component.scss
@@ -182,6 +182,152 @@ section h3 {
   margin: 0;
 }
 
+.student-progress-section h3 {
+  margin-bottom: 6px;
+}
+
+.student-progress-grid {
+  display: grid;
+  grid-template-columns: 1fr;
+  gap: 16px;
+}
+
+.student-progress-card {
+  min-height: 220px;
+}
+
+.skeleton-card {
+  pointer-events: none;
+}
+
+.skeleton-line {
+  height: 12px;
+  border-radius: 6px;
+  background: linear-gradient(90deg, #e9ecef 25%, #f8f9fa 50%, #e9ecef 75%);
+  background-size: 300% 100%;
+  animation: skeleton-shimmer 1.2s ease-in-out infinite;
+}
+
+.skeleton-title {
+  height: 18px;
+  width: 60%;
+}
+
+.skeleton-short {
+  width: 40%;
+}
+
+@keyframes skeleton-shimmer {
+  0% {
+    background-position: 100% 0;
+  }
+  100% {
+    background-position: -100% 0;
+  }
+}
+
+.student-progress-modal {
+  position: fixed;
+  inset: 0;
+  z-index: 1200;
+  display: flex;
+  justify-content: center;
+  align-items: center;
+  padding: 16px;
+}
+
+.student-progress-modal-backdrop {
+  position: absolute;
+  inset: 0;
+  background: rgba(0, 0, 0, 0.45);
+}
+
+.student-progress-modal-content {
+  position: relative;
+  width: min(860px, 100%);
+  max-height: 85vh;
+  overflow: auto;
+  background: #fff;
+  border-radius: 12px;
+  padding: 20px;
+  box-shadow: 0 20px 42px rgba(0, 0, 0, 0.22);
+}
+
+.chart-wrapper {
+  margin-top: 16px;
+}
+
+.status-message.warn {
+  background: #fff8e1;
+  border: 1px solid #ffd54f;
+  color: #8a6d00;
+}
+
+.attendance-actions {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 12px;
+  margin-bottom: 12px;
+  flex-wrap: wrap;
+}
+
+.attendance-filters {
+  display: flex;
+  gap: 10px;
+  align-items: end;
+  flex-wrap: wrap;
+}
+
+.attendance-filters label {
+  display: flex;
+  flex-direction: column;
+  gap: 4px;
+  font-size: 13px;
+  color: #495057;
+}
+
+.attendance-filters input {
+  border: 1px solid #d8dee4;
+  border-radius: 8px;
+  padding: 8px 10px;
+}
+
+.attendance-row {
+  display: flex;
+  gap: 12px;
+  align-items: center;
+}
+
+.attendance-status {
+  margin: 6px 0 0;
+  font-weight: 600;
+}
+
+.attendance-status.status-present {
+  color: #2e7d32;
+}
+
+.attendance-status.status-justified {
+  color: #455a64;
+}
+
+.attendance-status.status-absent {
+  color: #c62828;
+}
+
+.attendance-summary {
+  margin-bottom: 8px;
+}
+
+.attendance-pagination {
+  display: flex;
+  justify-content: space-between;
+  align-items: center;
+  gap: 12px;
+  margin-top: 12px;
+}
+
 .feature-card-soft {
   background: #f8fbff;
   border-color: #dbeafe;
@@ -374,6 +520,20 @@ section h3 {
   }
 }
 
+@media (min-width: 600px) and (max-width: 1199px) {
+  .student-progress-grid {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    gap: 20px;
+  }
+}
+
+@media (min-width: 1200px) {
+  .student-progress-grid {
+    grid-template-columns: repeat(2, minmax(0, 1fr));
+    gap: 24px;
+  }
+}
+
 @media (max-width: 600px) {
   .dashboard-header {
     padding: 12px;
@@ -403,4 +563,18 @@ section h3 {
   .user-avatar {
     flex-shrink: 0;
   }
+
+  .student-progress-grid {
+    gap: 16px;
+  }
+
+  .student-progress-modal-content {
+    padding: 16px;
+    max-height: 92vh;
+  }
+
+  .attendance-pagination {
+    flex-direction: column;
+    align-items: stretch;
+  }
 }
\ No newline at end of file
diff --git a/frontend/src/components/home/home.component.spec.ts b/frontend/src/components/home/home.component.spec.ts
index ac044c7..349ae73 100644
--- a/frontend/src/components/home/home.component.spec.ts
+++ b/frontend/src/components/home/home.component.spec.ts
@@ -5,6 +5,7 @@ import { BehaviorSubject, of } from 'rxjs';
 
 import { ApiService } from '../../services/api.service';
 import { AuthService } from '../../services/auth.service';
+import { StudentProgressChartComponent } from '../student-progress-chart/student-progress-chart.component';
 import { HomeComponent } from './home.component';
 
 describe('HomeComponent', () => {
@@ -24,7 +25,7 @@ describe('HomeComponent', () => {
         academy: { id: 'academy-1', name: 'Academia' },
       }),
     });
-    apiSpy = jasmine.createSpyObj<ApiService>('ApiService', ['listLinkedStudents']);
+    apiSpy = jasmine.createSpyObj<ApiService>('ApiService', ['listLinkedStudents', 'getStudentAttendanceHistory']);
     routerSpy = jasmine.createSpyObj<Router>('Router', ['navigate']);
 
     authSpy.getCurrentUser.and.returnValue({
@@ -48,9 +49,27 @@ describe('HomeComponent', () => {
       }) as any
     );
 
+    apiSpy.getStudentAttendanceHistory.and.returnValue(
+      of({
+        studentName: 'Aluna',
+        items: [
+          { sessionDate: '2026-03-19', turmaName: 'Judo Iniciante', status: 'present' },
+          { sessionDate: '2026-03-18', turmaName: 'Judo Iniciante', status: 'absent' },
+        ],
+        total: 2,
+        limit: 20,
+        offset: 0,
+        attendancePercentage: 65,
+        warningBelow70: true,
+        currentStreak: 5,
+        currentStreakDays: 35,
+        generatedAt: '2026-03-27T16:00:00.000Z',
+      }) as any
+    );
+
     await TestBed.configureTestingModule({
       declarations: [HomeComponent],
-      imports: [FormsModule],
+      imports: [FormsModule, StudentProgressChartComponent],
       providers: [
         { provide: AuthService, useValue: authSpy },
         { provide: ApiService, useValue: apiSpy },
@@ -63,6 +82,10 @@ describe('HomeComponent', () => {
     fixture.detectChanges();
   });
 
+  afterEach(() => {
+    localStorage.clear();
+  });
+
   it('lista filhos com anamnese pendente para o respons├ível', () => {
     expect(component.studentsWithoutHealthScreening.length).toBe(1);
     expect(component.studentsWithoutHealthScreening[0].studentName).toBe('Filho Um');
@@ -75,4 +98,84 @@ describe('HomeComponent', () => {
       queryParams: { returnTo: '/home' },
     });
   });
+
+  it('carrega dashboard de progresso do cache quando aluno est├í offline', () => {
+    const cachedDashboard = {
+      heading: 'Ol├í, Aluna!',
+      subheading: 'Seu progresso em jud├┤',
+      cards: {
+        evolucaoMes: {
+          currentMonthPresentCount: 4,
+          previousMonthPresentCount: 3,
+          delta: 1,
+          monthlySeries: [],
+          weeklySeries: [
+            { weekNumber: 1, date: '2026-03-01', proficiencyPercent: 45 },
+            { weekNumber: 2, date: '2026-03-08', proficiencyPercent: 52 },
+          ],
+        },
+        frequencia: {
+          presentCount90d: 4,
+          totalCount90d: 5,
+          attendancePercentage90d: 80,
+          recentSessions: [],
+        },
+        comentariosProfessor: {
+          totalComments: 0,
+          timeline: [],
+        },
+        faixaConquistas: {
+          isFederated: false,
+          totalBadges: 0,
+          latestBadges: [],
+          currentStreak: 0,
+          beltHistory: [],
+        },
+      },
+      generatedAt: '2026-03-27T16:00:00.000Z',
+    } as any;
+
+    localStorage.setItem(
+      'student-progress-dashboard:student-1',
+      JSON.stringify({
+        data: cachedDashboard,
+        cachedAt: '2026-03-27T16:00:00.000Z',
+      })
+    );
+
+    spyOnProperty(window.navigator, 'onLine', 'get').and.returnValue(false);
+
+    component.currentUser = {
+      id: 'student-1',
+      email: 'aluno@test.com',
+      fullName: 'Aluna',
+      role: 'Aluno',
+      academy: { id: 'academy-1', name: 'Academia' },
+    } as any;
+
+    component.loadStudentProgressDashboard();
+
+    expect(component.studentProgressDashboard?.heading).toBe('Ol├í, Aluna!');
+    expect(component.studentProgressWeeklyData.length).toBe(2);
+    expect(component.isUsingCachedProgressData).toBeTrue();
+    expect(component.hasStudentProgressError).toBeFalse();
+  });
+
+  it('abre detalhes de frequencia e carrega historico paginado com streak e warning', () => {
+    component.currentUser = {
+      id: 'student-1',
+      email: 'aluno@test.com',
+      fullName: 'Aluna',
+      role: 'Aluno',
+      academy: { id: 'academy-1', name: 'Academia' },
+    } as any;
+
+    component.openStudentProgressDetails('frequencia');
+
+    expect(apiSpy.getStudentAttendanceHistory).toHaveBeenCalled();
+    expect(component.attendanceHistory.length).toBe(2);
+    expect(component.attendanceHistoryWarningBelow70).toBeTrue();
+    expect(component.attendanceCurrentStreak).toBe(5);
+    expect(component.attendanceCurrentStreakDays).toBe(35);
+  });
 });
\ No newline at end of file
diff --git a/frontend/src/components/home/home.component.ts b/frontend/src/components/home/home.component.ts
index fcc58e9..2ec7861 100644
--- a/frontend/src/components/home/home.component.ts
+++ b/frontend/src/components/home/home.component.ts
@@ -1,10 +1,20 @@
 import { ChangeDetectorRef, Component, NgZone, OnInit, OnDestroy } from '@angular/core';
 import { Router } from '@angular/router';
-import { Subject } from 'rxjs';
+import { firstValueFrom, Subject } from 'rxjs';
 import { finalize, takeUntil, timeout } from 'rxjs/operators';
 import { AuthService } from '../../services/auth.service';
 import { ApiService } from '../../services/api.service';
-import { DeletionRequestItem, LinkedStudentItem, ProfessorTurmaItem, RecentTrainingSummary, User } from '../../types';
+import {
+  DeletionRequestItem,
+  LinkedStudentItem,
+  ProfessorTurmaItem,
+  RecentTrainingSummary,
+  StudentAttendanceHistoryItem,
+  StudentAttendanceHistoryResponse,
+  StudentProgressDashboardResponse,
+  StudentProgressWeekly,
+  User,
+} from '../../types';
 
 @Component({
   selector: 'app-home',
@@ -37,8 +47,31 @@ export class HomeComponent implements OnInit, OnDestroy {
   isSubmittingDeletion = false;
   isLoadingStatus = false;
   isLoadingLinkedStudents = false;
+  studentProgressDashboard: StudentProgressDashboardResponse | null = null;
+  isLoadingStudentProgress = false;
+  hasStudentProgressError = false;
+  studentProgressErrorMessage = '';
+  isUsingCachedProgressData = false;
+  lastProgressSyncAt: string | null = null;
+  selectedStudentProgressDetail: 'evolucao' | 'frequencia' | 'comentarios' | 'faixa' | null = null;
+  studentProgressWeeklyData: StudentProgressWeekly[] = [];
+  attendanceHistory: StudentAttendanceHistoryItem[] = [];
+  attendanceHistoryTotal = 0;
+  attendanceHistoryLimit = 20;
+  attendanceHistoryOffset = 0;
+  attendanceHistoryPercentage = 0;
+  attendanceHistoryWarningBelow70 = false;
+  attendanceCurrentStreak = 0;
+  attendanceCurrentStreakDays = 0;
+  attendanceFilterFrom = '';
+  attendanceFilterTo = '';
+  isLoadingAttendanceHistory = false;
+  attendanceHistoryErrorMessage = '';
 
   private destroy$ = new Subject<void>();
+  private studentProgressRetryTimer: ReturnType<typeof setTimeout> | null = null;
+  private readonly STUDENT_PROGRESS_RETRY_MS = 2000;
+  private readonly STUDENT_PROGRESS_TIMEOUT_MS = 10000;
 
   constructor(
     private auth: AuthService,
@@ -62,6 +95,10 @@ export class HomeComponent implements OnInit, OnDestroy {
     if (this.isProfessor()) {
       this.loadRecentTrainings();
     }
+
+    if (this.isAluno()) {
+      this.loadStudentProgressDashboard();
+    }
   }
 
   isResponsavel(): boolean {
@@ -188,6 +225,10 @@ export class HomeComponent implements OnInit, OnDestroy {
   }
 
   ngOnDestroy(): void {
+    if (this.studentProgressRetryTimer) {
+      clearTimeout(this.studentProgressRetryTimer);
+      this.studentProgressRetryTimer = null;
+    }
     this.destroy$.next();
     this.destroy$.complete();
   }
@@ -215,7 +256,7 @@ export class HomeComponent implements OnInit, OnDestroy {
       return 'Painel do Professor';
     }
     if (this.isAluno()) {
-      return '├ürea do Aluno';
+      return `Ol├í, ${this.currentUser?.fullName || 'Aluno'}!`;
     }
     if (this.isResponsavel()) {
       return '├ürea do Respons├ível';
@@ -228,7 +269,7 @@ export class HomeComponent implements OnInit, OnDestroy {
       return 'Acompanhe seus treinos, organize turmas e acesse o hist├│rico recente em um s├│ lugar.';
     }
     if (this.isAluno()) {
-      return 'Atualize seu cadastro, revise sua anamnese e acompanhe as pr├│ximas a├º├Áes do seu perfil.';
+      return 'Seu progresso em jud├┤';
     }
     if (this.isResponsavel()) {
       return 'Gerencie pend├¬ncias de sa├║de e acompanhe solicita├º├Áes LGPD dos alunos vinculados.';
@@ -359,6 +400,309 @@ export class HomeComponent implements OnInit, OnDestroy {
       });
   }
 
+  loadStudentProgressDashboard(forceRefresh: boolean = false): void {
+    if (!this.currentUser || !this.isAluno()) {
+      return;
+    }
+
+    if (this.studentProgressRetryTimer) {
+      clearTimeout(this.studentProgressRetryTimer);
+      this.studentProgressRetryTimer = null;
+    }
+
+    const cacheKey = this.getStudentProgressCacheKey();
+    const cached = this.readStudentProgressCache(cacheKey);
+
+    if (!navigator.onLine && cached) {
+      this.studentProgressDashboard = cached.data;
+      this.studentProgressWeeklyData = cached.data.cards.evolucaoMes.weeklySeries || [];
+      this.lastProgressSyncAt = cached.cachedAt;
+      this.isUsingCachedProgressData = true;
+      this.isLoadingStudentProgress = false;
+      this.hasStudentProgressError = false;
+      this.studentProgressErrorMessage = '';
+      return;
+    }
+
+    this.isLoadingStudentProgress = true;
+    this.hasStudentProgressError = false;
+    this.studentProgressErrorMessage = '';
+    this.isUsingCachedProgressData = false;
+
+    if (!forceRefresh && cached?.cachedAt) {
+      this.lastProgressSyncAt = cached.cachedAt;
+    }
+
+    const startedAt = Date.now();
+
+    const tryFetch = () => {
+      this.api.getStudentProgressDashboard().pipe(
+        timeout(3000),
+        takeUntil(this.destroy$)
+      ).subscribe({
+        next: (response) => {
+          this.studentProgressDashboard = response;
+          this.studentProgressWeeklyData = response.cards.evolucaoMes.weeklySeries || [];
+          this.isLoadingStudentProgress = false;
+          this.hasStudentProgressError = false;
+          this.studentProgressErrorMessage = '';
+          this.isUsingCachedProgressData = false;
+          this.lastProgressSyncAt = new Date().toISOString();
+          this.writeStudentProgressCache(cacheKey, response, this.lastProgressSyncAt);
+        },
+        error: () => {
+          const elapsed = Date.now() - startedAt;
+          if (elapsed < this.STUDENT_PROGRESS_TIMEOUT_MS && navigator.onLine) {
+            this.studentProgressRetryTimer = setTimeout(() => {
+              tryFetch();
+            }, this.STUDENT_PROGRESS_RETRY_MS);
+            return;
+          }
+
+          this.isLoadingStudentProgress = false;
+          this.hasStudentProgressError = true;
+          this.studentProgressErrorMessage = 'Erro ao carregar. Tentar novamente';
+        },
+      });
+    };
+
+    tryFetch();
+  }
+
+  openStudentProgressDetails(detail: 'evolucao' | 'frequencia' | 'comentarios' | 'faixa'): void {
+    this.selectedStudentProgressDetail = detail;
+
+    if (detail === 'frequencia') {
+      this.loadStudentAttendanceHistory(true);
+    }
+  }
+
+  closeStudentProgressDetails(): void {
+    this.selectedStudentProgressDetail = null;
+  }
+
+  getStudentProgressHeading(): string {
+    if (this.studentProgressDashboard?.heading) {
+      return this.studentProgressDashboard.heading;
+    }
+    return `Ol├í, ${this.currentUser?.fullName || 'Aluno'}!`;
+  }
+
+  getStudentProgressSubheading(): string {
+    return this.studentProgressDashboard?.subheading || 'Seu progresso em jud├┤';
+  }
+
+  getFormattedLastProgressSync(): string {
+    if (!this.lastProgressSyncAt) {
+      return '';
+    }
+
+    return new Date(this.lastProgressSyncAt).toLocaleString('pt-BR');
+  }
+
+  formatBeltDuration(days: number): string {
+    if (days <= 0) {
+      return 'Recente';
+    }
+
+    const months = Math.floor(days / 30);
+    if (months >= 1) {
+      return `${months} mes(es)`;
+    }
+
+    return `${days} dia(s)`;
+  }
+
+  get canLoadPreviousAttendancePage(): boolean {
+    return this.attendanceHistoryOffset > 0;
+  }
+
+  get canLoadNextAttendancePage(): boolean {
+    return this.attendanceHistoryOffset + this.attendanceHistoryLimit < this.attendanceHistoryTotal;
+  }
+
+  loadStudentAttendanceHistory(resetOffset: boolean = false): void {
+    if (!this.currentUser || !this.isAluno()) {
+      return;
+    }
+
+    if (resetOffset) {
+      this.attendanceHistoryOffset = 0;
+    }
+
+    this.isLoadingAttendanceHistory = true;
+    this.attendanceHistoryErrorMessage = '';
+
+    this.api.getStudentAttendanceHistory({
+      dateFrom: this.attendanceFilterFrom || undefined,
+      dateTo: this.attendanceFilterTo || undefined,
+      limit: this.attendanceHistoryLimit,
+      offset: this.attendanceHistoryOffset,
+    }).pipe(
+      takeUntil(this.destroy$)
+    ).subscribe({
+      next: (response: StudentAttendanceHistoryResponse) => {
+        this.attendanceHistory = response.items || [];
+        this.attendanceHistoryTotal = response.total || 0;
+        this.attendanceHistoryLimit = response.limit || 20;
+        this.attendanceHistoryOffset = response.offset || 0;
+        this.attendanceHistoryPercentage = response.attendancePercentage || 0;
+        this.attendanceHistoryWarningBelow70 = response.warningBelow70 === true;
+        this.attendanceCurrentStreak = response.currentStreak || 0;
+        this.attendanceCurrentStreakDays = response.currentStreakDays || 0;
+        this.isLoadingAttendanceHistory = false;
+      },
+      error: () => {
+        this.attendanceHistory = [];
+        this.isLoadingAttendanceHistory = false;
+        this.attendanceHistoryErrorMessage = 'Erro ao carregar hist├│rico de frequ├¬ncia';
+      },
+    });
+  }
+
+  applyAttendanceFilter(): void {
+    this.loadStudentAttendanceHistory(true);
+  }
+
+  clearAttendanceFilter(): void {
+    this.attendanceFilterFrom = '';
+    this.attendanceFilterTo = '';
+    this.loadStudentAttendanceHistory(true);
+  }
+
+  goToPreviousAttendancePage(): void {
+    if (!this.canLoadPreviousAttendancePage) {
+      return;
+    }
+    this.attendanceHistoryOffset = Math.max(0, this.attendanceHistoryOffset - this.attendanceHistoryLimit);
+    this.loadStudentAttendanceHistory(false);
+  }
+
+  goToNextAttendancePage(): void {
+    if (!this.canLoadNextAttendancePage) {
+      return;
+    }
+    this.attendanceHistoryOffset += this.attendanceHistoryLimit;
+    this.loadStudentAttendanceHistory(false);
+  }
+
+  getAttendanceStatusLabel(status: 'present' | 'absent' | 'justified'): string {
+    if (status === 'present') {
+      return 'Ô£ô Presente';
+    }
+    if (status === 'justified') {
+      return 'ÔÜ¬ Justificada';
+    }
+    return 'Ô£ù Ausente';
+  }
+
+  getAttendanceStatusClass(status: 'present' | 'absent' | 'justified'): string {
+    if (status === 'present') {
+      return 'status-present';
+    }
+    if (status === 'justified') {
+      return 'status-justified';
+    }
+    return 'status-absent';
+  }
+
+  getAttendanceRangeLabel(): string {
+    if (this.attendanceHistoryTotal === 0 || this.attendanceHistory.length === 0) {
+      return '0 de 0';
+    }
+    const start = this.attendanceHistoryOffset + 1;
+    const end = this.attendanceHistoryOffset + this.attendanceHistory.length;
+    return `${start}-${end} de ${this.attendanceHistoryTotal}`;
+  }
+
+  async exportAttendanceCsv(): Promise<void> {
+    if (!this.currentUser || !this.isAluno()) {
+      return;
+    }
+
+    const allItems: StudentAttendanceHistoryItem[] = [];
+    let total = 0;
+    let offset = 0;
+    const limit = 100;
+
+    do {
+      const response = await firstValueFrom(this.api.getStudentAttendanceHistory({
+        dateFrom: this.attendanceFilterFrom || undefined,
+        dateTo: this.attendanceFilterTo || undefined,
+        limit,
+        offset,
+      }));
+
+      const chunk = response.items || [];
+      total = response.total || chunk.length;
+      allItems.push(...chunk);
+
+      if (chunk.length === 0) {
+        break;
+      }
+
+      offset += response.limit || limit;
+    } while (offset < total);
+
+    if (!allItems.length) {
+      return;
+    }
+
+    const header = 'Data,Turma,Status\n';
+    const rows = allItems.map((item) => {
+      const date = new Date(item.sessionDate).toLocaleDateString('pt-BR');
+      const turma = `"${item.turmaName.replace(/"/g, '""')}"`;
+      const status = this.getAttendanceStatusLabel(item.status);
+      return `${date},${turma},${status}`;
+    }).join('\n');
+
+    const blob = new Blob([header + rows], { type: 'text/csv;charset=utf-8;' });
+    const url = window.URL.createObjectURL(blob);
+    const link = document.createElement('a');
+    const studentName = (this.currentUser?.fullName || 'Aluno').replace(/\s+/g, '_');
+    link.href = url;
+    link.download = `Frequencia_${studentName}_2026.csv`;
+    link.click();
+    window.URL.revokeObjectURL(url);
+  }
+
+  private getStudentProgressCacheKey(): string {
+    return `student-progress-dashboard:${this.currentUser?.id || 'unknown'}`;
+  }
+
+  private readStudentProgressCache(cacheKey: string): {
+    data: StudentProgressDashboardResponse;
+    cachedAt: string;
+  } | null {
+    try {
+      const raw = localStorage.getItem(cacheKey);
+      if (!raw) {
+        return null;
+      }
+
+      const parsed = JSON.parse(raw) as { data: StudentProgressDashboardResponse; cachedAt: string };
+      if (!parsed?.data || !parsed?.cachedAt) {
+        return null;
+      }
+
+      return parsed;
+    } catch {
+      return null;
+    }
+  }
+
+  private writeStudentProgressCache(
+    cacheKey: string,
+    data: StudentProgressDashboardResponse,
+    cachedAt: string | null
+  ): void {
+    if (!cachedAt) {
+      return;
+    }
+
+    localStorage.setItem(cacheKey, JSON.stringify({ data, cachedAt }));
+  }
+
   formatTrainingDate(dateStr: string): string {
     const [year, month, day] = dateStr.split('-').map(Number);
     return new Date(year, month - 1, day).toLocaleDateString('pt-BR');
